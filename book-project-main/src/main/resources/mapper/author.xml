<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bk.project.author.mapper.AuthorMapper">


<resultMap id="authorMap" type="Author">
<id property="authorNo"  column="author_no"/>
<result property="authorName" column="author_name"/>
<result property="nationality" column="nationality"/>
<result property="gender" column="gender"/>
<result property="birthDate" column="birth_date"/>
</resultMap>

	<insert id="insertAuthor" parameterType="Author">
		INSERT INTO author(author_name, nationality, gender, birth_date, contract_no)
		VALUES (#{authorName}, #{nationality}, #{gender}, #{birthDate}, #{contractNo})
	</insert>
	
<delete id="deleteBook" parameterType="Author">
DELETE FROM author
WHERE contract_no = #{contractNo};
</delete>

</mapper>
